apply plugin: 'org.springframework.boot'

bootJar {
    archiveBaseName = 'discovery-service'
    version = '0.0.1'
}

dependencies {
    compile libs.eurekaServer,
            libs.gson,
            libs.jaxb,
            libs.jaxbruntime
}