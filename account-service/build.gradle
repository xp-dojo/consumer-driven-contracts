bootJar {
    baseName = 'trade-pricer'
    version = '0.0.1'
}

dependencies {
    compile libs.springBootStarterWeb,
            libs.eurekaClient,
            libs.gson,
            libs.swagger,
            libs.swaggerUI

    runtime libs.jacksonDatatypeJsr310

    testCompile libs.test.pactJvmProvider,
            libs.test.pactSpring

    runtime project(':discovery-service')
}
